[project]
name = "aegis-crew"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyautogen==0.2.35",
    "python-dotenv>=1.0.1",
    "httpx>=0.27.0",
    "openai>=1.3,<2",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
